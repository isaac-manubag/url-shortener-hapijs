version: '3.5'
services:
  hapi:
    container_name: hapi
    build:
      context: '.'
    ports:
      - '3000:3000'
    volumes:
      - .:/usr/src/app
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - ./data:/data/db
    ports:
      - '27017:27017'
